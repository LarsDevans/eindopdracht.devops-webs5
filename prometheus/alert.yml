groups:
  - name: Alerts
    rules:
      - alert: "API_Down"
        expr: up{job="microservices"} < 1
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "Microservice is down"
          description: "The microservice has been unreachable for more than 30 seconds."
