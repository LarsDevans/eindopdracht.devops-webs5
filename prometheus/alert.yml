groups:
  - name: Alerts
    rules:
      - alert: "API_Down"
        expr: up{job="microservices"} < 1
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "API Gateway is down"
          description: "The API Gateway has been unreachable for more than 30 seconds."
